/*
One needs to download node.js and install it on their computer.
They then go ahead to install the extensions in Javascript that would enable VSCode to interprete the code and debug it.
Lastly, one would need to install a Code Runner that allows the VSCode to display the results from the code.
They can then go ahead to open a folder and create a new file with a .js extension and code away.
 */
